name: oa_klg_topk
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.7
  #- pytorch>=2.0.0
  #- torchvision
  #- torchaudio
  #- cudatoolkit=11.8  # Adjust based on your CUDA version
  - numpy>=1.19.0,<1.24.0  # Python 3.7 compatible versions
  - pandas>=1.3.0,<2.0.0  # Python 3.7 compatible
  - scikit-learn>=1.0.0,<1.2.0  # Python 3.7 compatible
  - scipy>=1.7.0,<1.10.0  # Python 3.7 compatible
  - matplotlib
  - seaborn
  - jupyter
  - ipython
  - pip
  - pip:
    # nnU-Net v2
    - nnunetv2>=2.2.0
    
    # PyTorch Radiomics - install from GitHub
    #- git+https://github.com/AIM-Harvard/pytorch-radiomics.git
    
    # Medical imaging
    - SimpleITK>=2.2.0
    - nibabel>=3.2.0
    
    # Data handling
    - openpyxl>=3.0.0  # For reading Excel files
    - pyarrow>=5.0.0  # For parquet support
    
    # Utilities
    - tqdm
    - batchgenerators  # Required by nnU-Net
    
    # Optional but recommended
    - tensorboard
    - wandb  # For experiment tracking (optional)

